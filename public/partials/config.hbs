<script>
  require( ['config'], function( config ){
    config.cdn = { baseUrl: '{{{config.cdn.baseUrl}}}' };
    config.deliveryTime = {{{json config.deliveryTime}}};
    config.baseUrl = '{{config.baseUrl}}';
    config.availableRestaurantPlanTypes = '{{config.availableRestaurantPlanTypes}}';
    config.disallowOrdersBetween = {{{json config.disallowOrdersBetween}}};
  });
</script>