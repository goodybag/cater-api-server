var components = module.exports = {
  /**
   * Imports specified component and attaches it to dom
   *
   * @param path    The component path
   * @param target  Attach to a query selector, DomElement, or null
   * @param logger  Parent logger
   * @return component
   */
  init: function( path, target, logger) {
    
  }
}