<div class="btn-group-vertical status-buttons">
  <a href="?p={{p}}" class="btn btn-default {{#eq status "all"}}active{{/eq}}" data-status="all">
    All
  </a>
  {{#unless isRestaurantManager}}
  <a href="?status=pending&p={{p}}" class="btn btn-pending {{#eq status "pending"}}active{{/eq}}">
    Pending
  </a>
  {{/unless}}
  {{#unless isRestaurantManager}}
  <a href="?status=canceled&p={{p}}" class="btn btn-canceled {{#eq status "canceled"}}active{{/eq}}">
    Canceled
  </a>
  {{/unless}}
  <a href="?status=submitted&p={{p}}" class="btn btn-submitted {{#eq status "submitted"}}active{{/eq}}">
    Submitted
  </a>
  <a href="?status=accepted&p={{p}}" class="btn btn-accepted {{#eq status "accepted"}}active{{/eq}}">
    Accepted
  </a>
  <a href="?status=denied&p={{p}}" class="btn btn-denied {{#eq status "denied"}}active{{/eq}}">
    Denied
  </a>
  {{#unless isRestaurantManager}}
  <a href="?status=delivered&p={{p}}" class="btn btn-delivered {{#eq status "delivered"}}active{{/eq}}">
    Delivered
  </a>
  {{/unless}}
</div>
