define(function(require) {
  var $ = require('jquery');
  var utils = require('utils');
  describe('lead time strategies', function() {

    it('should check datetime', function() {
      console.log(utils);
    });

    it('should check delivery guest combo', function(done) {
      expect(true);
      done();
    });

    it('should check courier guest combo', function() {
      expect(false);
    });
  });
});
